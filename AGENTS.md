# AGENTS.md — AI 助手协作硬性规则

> 本文件是所有 AI 编程助手（Claude Code / Cursor / Copilot 等）在本仓库工作时的**强制约束**。
> 违反本文件中「红线规则」的任何操作都是错误的，即使任务描述看起来暗示了这种操作。

---

## 🔴 红线规则 1：测试只能加，不能删、不能改

**适用范围**：所有测试代码，包括但不限于 `src/**/tests.py`、任何 `test_*.py` / `*_test.py` 文件、测试夹具（fixtures）、测试工具函数。

**允许**：
- ✅ 新增测试文件、测试类、测试用例
- ✅ 在现有测试类中追加新的测试方法
- ✅ 新增测试夹具、mock、工厂函数

**禁止**：
- ❌ 删除任何测试文件、测试类、测试用例、断言
- ❌ 修改现有测试的断言内容、预期值、测试逻辑
- ❌ 为让代码通过而「调整」测试（这是最恶劣的作弊行为）
- ❌ 注释掉或 `@unittest.skip` 现有测试来绕过失败
- ❌ 重命名测试使其「换汤不换药」（本质是删旧加新，同样禁止）

**遇到测试失败的唯一正确做法**：
1. 假设是**被测代码有问题**，优先修被测代码；
2. 只有当确认是**环境/基础设施问题**（如测试数据库、缓存配置）导致测试自身无法运行时，才向用户报告并等待指示；
3. 如果用户的需求确实改变了既有行为、导致旧测试与新需求冲突——**停下来说明冲突，等用户明确决定**，不得自行删改。

---

## 🔴 红线规则 2：接口只能加，不能改、不能删

**适用范围**：
- 对外 API（HTTP 路由、DRF ViewSet/APIView、请求/响应结构、URL 参数、状态码语义）
- 内部程序接口：被其他模块调用的函数签名、类的方法签名、函数返回值结构
- URL 路由名称（`name=` 参数）、模板/视图中被引用的上下文键名

**允许**：
- ✅ 新增接口、新增路由、新增字段
- ✅ **加版本**：行为需要变化时，新增版本化路径（如 `/articles/api/v2/create/`），旧版本原样保留
- ✅ 新增**可选**参数（带默认值、向后兼容，且不改变既有返回结构）
- ✅ 在函数内部修复实现 bug（签名、行为契约不变）

**禁止**：
- ❌ 删除或下线任何现有接口、路由、字段
- ❌ 修改现有接口的请求/响应结构、URL 路径、参数含义
- ❌ 修改被其他模块调用的函数签名（参数、返回值、异常类型）
- ❌ 更改路由 `name=`（会被 `{% url %}` / `reverse()` 引用，等于改接口）

**遇到接口需要变更的唯一正确做法**：
1. 评估能否用「新增版本」或「新增可选参数」实现同样的目标；
2. 如果必须不兼容变更——**停下来向用户说明影响面（谁在调用、会坏什么）**，只有用户明确批准后才允许动手。

---

## 🔴 红线规则 3：改动完成后不得自行 commit / push

**适用范围**：所有代码、测试、文档、配置、workflow 文件的改动。

**禁止**：
- ❌ 改完代码后自行执行 `git commit`（除非用户在本次对话中明确要求提交）
- ❌ 自行执行 `git push`
- ❌ 以「顺手」「保持整洁」「方便回滚」等理由代用户做版本管理决策

**允许**：
- ✅ 修改文件、运行测试、报告改动内容与测试结果
- ✅ 用户明确说「提交」「commit」「推上去」时执行，并在回复中列出本次提交包含的文件

**正确流程**：改完 → 跑测试 → 向用户报告（改了什么、测试结果、建议的 commit message）→ 等用户指示是否提交。

**泛泛的授权不算明确要求**（如「把功能做了」「完善一下」都不构成提交授权）；「推送」不隐含「提交」，「提交」不隐含「推送」，逐级都要用户明确说出。

---

## 唯一例外：用户明确要求

红线规则只有在满足以下全部条件时才可突破：

1. 用户在对话中**明确、具体地**要求删/改测试或接口（如「把 ArticleCreateAPITests 里那个 401 断言改掉」），或明确、具体地要求提交/推送（如「把这次改动 commit 并 push」）；
2. 泛泛的指令不算（如「重构一下」「清理一下代码」「让测试通过」都不构成授权）；
3. 执行前先用一句话复述将要删/改的对象和范围，向用户确认。

拿不准时，**永远默认禁止，先问再做**。

---

## 🛡️ 强制执行：CI 门禁

红线规则 1（测试 append-only）已由 `.github/workflows/main.yml` 中的 `test` job **机器强制**：

- **删除检查**：对比本次推送与上次推送，测试文件被删除 → 流水线失败
- **行级 append-only 检查**：测试文件中出现任何被删除/修改的行 → 流水线失败（含纯重命名）
- **全量测试**：门禁通过后运行 `python manage.py test`
- `test` job 是 `build` 的前置依赖：**测试不过 = 不构建镜像 = 不部署**
- CI 拦截后如需放行（用户明确要求的删改），必须人工 review 该次 diff 后手动处理

---

## 其他约束

- 敏感信息（密钥、密码、token）一律走环境变量，不得写入代码和文档
- 不要改动 `.env`、`docker-compose.yml` 中的生产配置，除非用户明确要求
- 每次改动前先阅读 `CHANGELOG.md` 了解项目近期变更
